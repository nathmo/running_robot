# DASH-01 sprint training stack. Install into a fresh venv:
#   python -m venv .venv && .venv/Scripts/activate  (Windows)  |  source .venv/bin/activate (Linux)
#   pip install -r training/requirements.txt
# On the Izar cluster GPUs (V100, sm_70) install torch from the cu126 index instead — see
# training/slurm/README.md (recent default wheels dropped Volta support; CPU torch also works,
# the bottleneck is the MuJoCo envs, not the MLP).
mujoco>=3.2
stable-baselines3>=2.3
gymnasium>=0.29
torch>=2.2
numpy>=1.26
tensorboard>=2.15
matplotlib>=3.8
imageio>=2.34
imageio-ffmpeg>=0.4.9
pillow>=10.0
tqdm>=4.66
rich>=13.0
