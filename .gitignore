# Large training artifacts
models/
logs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
# virtualenvs — match at any depth
.venv/
venv/
**/.venv/
**/venv/
*/__pycache__/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.claude/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Temporary
*.tmp
*.log
# MuJoCo's own warning log, written next to whatever script ran (trailing comments are NOT
# supported in .gitignore, hence the separate line)
MUJOCO_LOG.TXT

# scratch validation renders
training/model/_*.png
robot/model/_*.png
robot/fixed_gait/calibration/_selftest/

# web UI runtime data (session calibration, saved workspaces/gaits) — machine-local
robot/fixed_gait/webui/data/

# RL run intermediates — keep only final model + summary artifacts
training/runs/**/ppo_*_steps.zip
training/runs/**/ppo_vecnormalize_*_steps.pkl
training/runs/**/events.out.tfevents.*
training/runs/**/*.mp4

# archived pre-2026-07-17 run outputs (moved out of the deleted rl/ tree; delete when done)
old_runs/

# training run outputs: checkpoints/videos are pulled on demand, not versioned
training/runs/
