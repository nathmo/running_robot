# CPU-only PyTorch. The training server (driver 470 / Tesla K20Xm Kepler
# sm_35) cannot run any torch>=1.13 on GPU: its driver caps at CUDA 11.4,
# and sm_35 was dropped from PyTorch long before 1.13. For the small MLP
# policies used here, CPU torch is as fast as a K20 would have been anyway.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu

mujoco>=3.1.0
numpy<2.0.0
scipy
matplotlib
stable-baselines3>=2.0.0
gymnasium>=0.28.0
perlin-noise
pyyaml
tensorboard
tqdm
stable-baselines3[extra]
onnx>=1.16.0
onnxruntime>=1.18.0