# SpiderBot RL dependencies — CPU only, no GPU required.
# Install torch first from the CPU index, then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r rl/requirements.txt
# (Tested versions in comments; floors kept loose for portability.)

mujoco>=3.1            # tested 3.7.0   — physics + offscreen renderer
gymnasium>=0.29        # tested 1.3.0   — RL environment API
stable-baselines3>=2.3 # tested 2.8.0   — PPO
torch>=2.2             # tested 2.11.0  — install the CPU build (see note above)
numpy>=1.26            # tested 2.x
tensorboard>=2.15      # tested 2.20.0  — training curves
matplotlib>=3.7        # training_plots.png (loss/score/reward-term curves)
tqdm>=4.66             # progress bar
rich>=13.0             # progress bar
imageio>=2.34          # mp4 rollouts (evaluate.py --video)
imageio-ffmpeg>=0.4    # mp4 encoder
Pillow>=10.0           # render PNGs

# for ONNX export + Raspberry Pi deployment (later milestones; not needed to train)
onnx>=1.16
onnxruntime>=1.18      # tested 1.25.1
